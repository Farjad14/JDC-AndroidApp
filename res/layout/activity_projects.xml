<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:orientation="vertical"
    android:scrollbarStyle="insideInset"
    android:scrollbars="vertical" >

    <TextView
        android:id="@+id/projects"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="Projects"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:textColor="#254117"
        android:textStyle="bold" />

    <ScrollView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" >

        <TextView
            android:id="@+id/activityprojects"
            android:layout_width="318dp"
            android:layout_height="266dp"
            android:layout_marginTop="20dp"
            android:maxHeight="266dp"
            android:minHeight="266dp"
            android:text="@string/projectsinfo"
            android:textColor="#000000"
            android:textSize="15sp" />
    </ScrollView>

    <ImageView
        android:id="@+id/imageView1"
        android:layout_width="310dp"
        android:layout_height="114dp"
        android:layout_weight="1"
        android:src="@drawable/ambulance" />

    <Button
        android:id="@+id/btn_goback_projects"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/mission_vision_back" />

</LinearLayout>